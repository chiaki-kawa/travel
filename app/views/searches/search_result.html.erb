<h2>Results index</h2>

<table class="table table-hover table-inverse">
  <tbody>
    <% @posts.each do |post| %>
      <tr>
        <td><%= link_to post.title, post_path(post) %></td>
        <td><%= post.body%></td>
      </tr>
    <% end %>
  </tbody>
</table>